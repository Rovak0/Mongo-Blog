const Thought = require('./thought');
const User = require('./user');
const reaction = require('./reaction'); //lowercase because it isn't a full model

module.exports = {Thought, User, reaction };

//